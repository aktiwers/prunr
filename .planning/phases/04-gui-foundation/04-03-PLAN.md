---
phase: 04-gui-foundation
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: []
autonomous: false
requirements: [LOAD-01, LOAD-02, OUT-01, OUT-02, UX-01, UX-03, UX-04]
must_haves:
  truths:
    - "User has verified all 7 phase requirements work end-to-end in the real GUI"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete GUI foundation.

Purpose: Verify all Phase 4 requirements work in the real application with actual images and inference.
Output: Confirmed working GUI or list of issues to fix.
</objective>

<execution_context>
@/home/bolli/.ccs/instances/work/get-shit-done/workflows/execute-plan.md
@/home/bolli/.ccs/instances/work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-gui-foundation/04-CONTEXT.md
@.planning/phases/04-gui-foundation/04-UI-SPEC.md
@.planning/phases/04-gui-foundation/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Complete GUI foundation end-to-end</name>
  <files></files>
  <action>
    Launch the GUI application and verify all Phase 4 requirements work end-to-end.
    This is a human verification checkpoint -- no code changes needed.
    Run: `cargo run -p bgprunr-app --features dev-models`
  </action>
  <read_first>
    - .planning/phases/04-gui-foundation/04-02-SUMMARY.md
  </read_first>
  <what-built>
    Complete egui GUI with:
    - Dark theme window (1280x800 default)
    - Toolbar: Open Image, Remove BG, Save PNG, Copy Image buttons + Model selector
    - Canvas: drop zone in empty state, image display, checkerboard behind result
    - Status bar: state text, progress bar during inference, backend badge
    - Shortcuts overlay (? key)
    - Worker thread inference with cancel (Escape)
    - Drag-and-drop image loading
    - File dialogs (open/save via rfd)
    - Clipboard copy via arboard
  </what-built>
  <how-to-verify>
    Launch the app:
    ```
    cd /media/bolli/5c607cb1-5a5c-4c1b-a74a-d3060d86c222/Coding/Vibe/Private/BgPrunr
    cargo run -p bgprunr-app --features dev-models
    ```

    **Test 1 - Empty state (LOAD-01, LOAD-02):**
    1. Window opens at ~1280x800 with dark theme
    2. Drop zone visible with "Drop an image here" text
    3. Title bar shows "BgPrunR"
    4. Status bar shows "Ready" and a backend badge (CPU/CUDA)

    **Test 2 - Drag and drop (LOAD-01):**
    1. Drag any PNG/JPEG onto the window
    2. Image appears in the canvas, scaled to fit
    3. Title bar updates to "BgPrunR - filename.ext"
    4. Remove BG button becomes enabled (blue/accent)

    **Test 3 - File picker (LOAD-02):**
    1. Press Ctrl+O (or Cmd+O on macOS)
    2. Native file dialog opens with image filter
    3. Select an image -- it loads in the canvas

    **Test 4 - Background removal (UX-01, UX-03):**
    1. Press Ctrl+R (or click Remove BG)
    2. Status bar shows progress: "Processing... Preprocessing" then "Inferring" then "Applying alpha"
    3. UI remains responsive during processing (window is not frozen)
    4. Result appears with checkerboard behind transparent areas
    5. Save PNG and Copy Image buttons become enabled

    **Test 5 - Cancel (UX-03):**
    1. Load a new image, press Ctrl+R to start processing
    2. While processing, press Escape
    3. Processing stops, status shows "Cancelled"
    4. Image reverts to loaded (original) state

    **Test 6 - Save (OUT-01):**
    1. After successful removal, press Ctrl+S
    2. Save dialog appears with "result.png" default name
    3. Save to a location -- verify the file is a valid transparent PNG

    **Test 7 - Copy (OUT-02):**
    1. After successful removal, press Ctrl+C
    2. Status bar briefly shows "Copied to clipboard"
    3. Open another app (e.g., GIMP, browser) and paste -- image appears

    **Test 8 - Shortcuts overlay (UX-04):**
    1. Press ? key
    2. Centered overlay appears with "Keyboard Shortcuts" title
    3. Shows 6 shortcuts with correct platform modifiers
    4. Press ? or Escape to dismiss

    **Test 9 - Model selector:**
    1. Toolbar shows "Model: silueta (fast)" dropdown
    2. Can switch to "u2net (quality)"
    3. Removing BG uses the selected model
  </how-to-verify>
  <verify>
    <automated>cd /media/bolli/5c607cb1-5a5c-4c1b-a74a-d3060d86c222/Coding/Vibe/Private/BgPrunr && cargo build -p bgprunr-app --features dev-models 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Application launches without panic
    - All 9 manual verification tests pass as described in how-to-verify
    - cargo build -p bgprunr-app --features dev-models exits 0
  </acceptance_criteria>
  <done>Human has confirmed all Phase 4 requirements (LOAD-01, LOAD-02, OUT-01, OUT-02, UX-01, UX-03, UX-04) work correctly in the real GUI application</done>
  <resume-signal>Type "approved" if all tests pass, or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
Human has confirmed all 7 phase requirements working in the real GUI.
</verification>

<success_criteria>
- All 9 verification tests pass
- UI is responsive during inference
- Save produces valid transparent PNG
- Clipboard paste works in external app
- No panics or crashes during normal usage
</success_criteria>

<output>
After completion, create `.planning/phases/04-gui-foundation/04-03-SUMMARY.md`
</output>
