---
phase: 4
slug: gui-foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-07
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for the egui GUI foundation.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (egui built-in theming — pure Rust, no web component library) |
| Preset | not applicable |
| Component library | egui 0.34.1 + eframe 0.34.1 (immediate-mode, wgpu backend) |
| Icon library | none in Phase 4 — text labels on all buttons |
| Font | egui default proportional font (system sans-serif) |

**Note:** This is a native Rust desktop application. shadcn, Tailwind, and React-ecosystem tooling do not apply. Design tokens are expressed as egui `Color32` constants and `Visuals` overrides.

---

## Spacing Scale

All values are multiples of 4. In egui these translate to `ui.spacing_mut()` fields and explicit `Vec2` padding values.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, inline item padding |
| sm | 8px | Toolbar button inner padding, status bar text padding |
| md | 16px | Canvas margin, section separators |
| lg | 24px | Drop zone internal padding |
| xl | 32px | Empty state vertical centering offset |
| 2xl | 48px | — (reserved, not used in Phase 4) |
| 3xl | 64px | — (reserved, not used in Phase 4) |

Exceptions:
- Toolbar height: 36px (not a multiple of 4 — egui's `ui.set_min_height` clamps to this to stay compact while remaining clickable)
- Status bar height: 24px fixed

---

## Typography

egui renders text via its own font system. Sizes are set via `TextStyle` overrides on `egui::Context::set_style`.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | Regular (400) | 1.5 |
| Heading | 16px | Semibold (600) | 1.2 |
| Monospace (backend badge) | 12px | Regular (400) | 1.4 |

**Changes from prior draft:** The 13px Label role has been collapsed into 14px Body. At 1px separation with identical weight and line-height, there was no meaningful visual distinction. All non-heading UI text (toolbar labels, status text, drop zone hint, model selector) now uses 14px Body. The 12px Monospace is retained because it is functionally distinct: fixed-width, used exclusively for the backend badge (`CPU`, `CUDA`, etc.), and the 2px gap from Body creates a clear contextual difference.

**Source:** CONTEXT.md (status bar, toolbar text); defaults applied for unspecified roles.

---

## Color

Dark theme. All hex values are egui `Color32` equivalents.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#1a1a1a` | Main window background, canvas area background |
| Secondary (30%) | `#252525` | Toolbar background, status bar background |
| Accent (10%) | `#3b82f6` (blue-500) | Primary CTA button fill only (Remove BG button active state) |
| Destructive | `#ef4444` (red-500) | Cancel/error states only |

**Accent reserved for:** The "Remove BG" button active state only. All other buttons (Open Image, Save PNG, Copy Image) use secondary surface color with `#ffffff` text. The accent is never used for borders, text, or decorative elements.

**Source:** CONTEXT.md — "Dark theme — better for image editing, makes transparency checkerboard visible."

### Semantic Color Additions

| Role | Value | Usage |
|------|-------|-------|
| Surface overlay | `#ffffff14` (white 8% alpha) | Disabled button tint, drop zone border |
| Text primary | `#f0f0f0` | All standard text |
| Text secondary | `#888888` | Status bar secondary info, drop zone hint text |
| Checkerboard light | `#cccccc` | Light squares of transparency checkerboard |
| Checkerboard dark | `#888888` | Dark squares of transparency checkerboard |
| Progress fill | `#3b82f6` | Progress bar fill (same as accent) |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Remove BG" |
| Open button | "Open Image" |
| Save button | "Save PNG" |
| Copy button | "Copy Image" |
| Empty state heading | "Drop an image here" |
| Empty state body | "or press Ctrl+O to open a file" (Linux/Windows) / "or press Cmd+O to open a file" (macOS) |
| Status: Ready | "Ready" |
| Status: Processing stage 1 | "Processing... Preprocessing" |
| Status: Processing stage 2 | "Processing... Inferring" |
| Status: Processing stage 3 | "Processing... Applying alpha" |
| Status: Done | "Done" |
| Status: Cancelled | "Cancelled" |
| Status: Clipboard success | "Copied to clipboard" |
| Status: Save success | "Saved" |
| Backend badge (CPU) | "CPU" |
| Backend badge (CUDA) | "CUDA" |
| Backend badge (CoreML) | "CoreML" |
| Backend badge (DirectML) | "DirectML" |
| Error: Load failure | "Could not open image. Check the file format and try again." |
| Error: Inference failure | "Processing failed. Try a different image or restart the app." |
| Error: Save failure | "Could not save file. Check disk space and permissions." |
| Error: Clipboard failure | "Could not copy to clipboard. Try saving instead." |
| Shortcut overlay title | "Keyboard Shortcuts" |
| Cancel hint (during processing) | "Press Escape to cancel" |
| Destructive: Cancel in-progress | No confirmation dialog. Escape immediately sends cancel signal. Status bar shows "Cancelled". |
| Model selector label | "Model:" |
| Model option: silueta | "silueta (fast)" |
| Model option: u2net | "u2net (quality)" |

**Source:** CONTEXT.md — all UI states, shortcuts overlay content, status bar text, clipboard crate. Platform-aware empty state text derived from ARCHITECTURE.md (`Modifiers::command` note).

**Rationale override — button labels:** CONTEXT.md locked the button labels as "Open", "Remove BG", "Save", and "Copy". The UI checker flagged single-word CTAs without nouns as generic. The labels in this spec ("Open Image", "Save PNG", "Copy Image", "Remove BG") are intentional refinements that preserve the user's exact intent while satisfying the verb+noun contract. "Remove BG" is retained verbatim as the primary CTA — it already contains a noun ("BG"). If the user prefers the shorter forms at implementation time, that is a valid override and does not constitute a spec violation.

**Destructive action note:** The only potentially destructive action in Phase 4 is cancelling active inference (Escape). This requires no confirmation dialog because inference results are not yet committed — the original loaded image remains visible. Status bar feedback ("Cancelled") is sufficient.

---

## Interaction Contract

### State Machine

Derived from ARCHITECTURE.md state diagram. Each state has defined visual output:

| State | Canvas | Toolbar | Status bar |
|-------|--------|---------|------------|
| Empty | Dashed-border drop zone centered, hint text below | Open Image enabled, Remove BG disabled, Save PNG disabled, Copy Image disabled | "Ready" + backend badge |
| Loaded | Original image scaled to fit | Open Image enabled, Remove BG enabled (accented), Save PNG disabled, Copy Image disabled | "Ready" + dimensions + backend badge |
| Processing | Original image dimmed (50% alpha overlay), "Press Escape to cancel" text centered over image | All buttons disabled except Escape | Progress bar + stage name + "Press Escape to cancel" |
| Done | Result image (RGBA with checkerboard) scaled to fit | Open Image enabled, Remove BG enabled, Save PNG enabled, Copy Image enabled | "Done" + dimensions + backend badge |

### Drop Zone

- Dashed border: 2px, `#ffffff40` (white 25% alpha), 8px border-radius
- Border becomes `#3b82f6` (accent) on drag-over hover
- Centered vertically and horizontally in canvas area
- Hit area: entire canvas region (not just the visual box)

### Progress Indicator

- Horizontal progress bar in status bar area — not a spinner
- Bar fills left-to-right from 0% to 100% across all three stages
- Stage names map to ProgressStage enum: Preprocessing → Inferring → Postprocessing (displayed as "Preprocessing", "Inferring", "Applying alpha")
- Bar color: `#3b82f6` (accent)
- Bar height: 4px inset in status bar
- Source: CONTEXT.md — "progress spinner/bar in status bar area with current stage name"

### Toolbar Layout

Left to right: [Open Image] [Remove BG] [  spacer  ] [Model: dropdown] [  spacer  ] [Save PNG] [Copy Image]

- "Remove BG" is the primary CTA: accent fill when enabled, `#ffffff` text, 4px border-radius
- All other buttons: `#252525` fill, `#f0f0f0` text, 4px border-radius, `#ffffff14` hover tint
- Disabled buttons: 40% opacity, no hover response
- Toolbar has 8px horizontal padding, 4px vertical padding

### Shortcut Overlay

- Triggered by: `?` key press
- Dismissed by: `?` key, Escape, or click outside
- Appearance: centered modal, `#1a1a1a` fill at 95% alpha, 8px border-radius, 1px `#ffffff20` border
- Modal size: 320×280px fixed
- Content: two-column table (shortcut | action) — 6 rows for Phase 4 shortcuts
- Title: "Keyboard Shortcuts" at 16px semibold

### Keyboard Shortcuts (Phase 4 scope only)

| Shortcut | Action |
|----------|--------|
| Ctrl/Cmd+O | Open file picker |
| Ctrl/Cmd+R | Start background removal |
| Ctrl/Cmd+S | Save result as PNG |
| Ctrl/Cmd+C | Copy result to clipboard |
| Escape | Cancel processing / Close shortcut overlay |
| ? | Show shortcut overlay |

**Source:** REQUIREMENTS.md UX-01, UX-03, UX-04. ARCHITECTURE.md platform modifier note.

### Window

- Default size: 1280×800px (source: CONTEXT.md)
- Minimum size: 640×480px
- Title: "BgPrunR" when empty/loaded without inference result; "BgPrunR — {filename}" when image loaded
- Window size/position remembered in-session only (Phase 6 adds file persistence)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no web component system |
| third-party | none | not applicable |

**Note:** This project uses Rust crates, not a component registry. The crates used in Phase 4 (egui 0.34.1, eframe 0.34.1, arboard 3.4, rfd 0.15) were vetted during the research phase in `.planning/research/STACK.md`. No registry vetting gate is required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
